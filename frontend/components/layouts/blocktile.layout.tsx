import TextInput from "../ui/formfield.component"
export default function BlockTile({ id, field }: { id: string, field: string }) {
    return (
        <div>
            Tiles
        </div>
    )
}  